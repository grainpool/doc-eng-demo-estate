<!-- GENERATED BY CONCORD — do not edit. Source: fixtures/cli-introspection.json (T2_CLI). -->
# relay config

## config

Inspect CLI configuration and API health.

```
Usage: relay config [options] [command]

Show the resolved CLI configuration and check the Relay API.

Options:
  -h, --help      display help for command

Commands:
  show            Show resolved configuration
  status          Check the Relay API health
  help [command]  display help for command

Examples:
  $ relay config show
  $ relay config status
```

Examples:

```
$ relay config show
```
```
$ relay config status
```


## config show

Show resolved configuration.

```
Usage: relay config show [options]

Show the API URL and whether a token is set (never the token itself).

Options:
  -h, --help  display help for command

Examples:
  $ relay config show
```

Examples:

```
$ relay config show
```


## config status

Check the Relay API health.

```
Usage: relay config status [options]

Call GET /api/health and summarize the five checks.

Options:
  -h, --help  display help for command

Examples:
  $ relay config status
```

Examples:

```
$ relay config status
```

