# Using the CLI

Prefer a terminal? The `relay` command does everything the web app does:
create projects, upload files, run analysis sessions, download artifacts.

Add `--json` to any command for machine-readable output, and rely on the exit
codes in scripts — they're part of the CLI's contract. Run `relay --help` to
see every command with examples.
