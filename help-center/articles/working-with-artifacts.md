# Working with artifacts

Every result — table, plot, summary — is saved as an artifact on the project.

Open an artifact to see where it came from: the source file, the operation and
its parameters, the package versions used, and how long it took. If it was
computed from another result (a filtered table, say), the lineage section shows
the chain.

Use **Download** to get the CSV, PNG, or JSON.
